apiVersion: v1
kind: ConfigMap
metadata:
  name: agent-config
  namespace: __NAMESPACE__
data:
  # 模型调用走集群内 llm-service（凭据在其自身 Secret 里），这里只放本服务的检索参数。
  LITERATURE_LLM_ENABLED: "true"
  LITERATURE_LLM_TIMEOUT: "30"
  LITERATURE_LLM_MAX_CANDIDATES: "40"
