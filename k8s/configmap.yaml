apiVersion: v1
kind: ConfigMap
metadata:
  name: panghu-agent-config
  namespace: panghu-agent
data:
  PROVIDER: "openai"
  API_PORT: "8000"
  ADMIN_EMAIL: "admin@localhost"
  DATABASE_URL: "sqlite:////app/data/data.db"
